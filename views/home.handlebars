<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Homepage</title>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <link rel="stylesheet" href="home.css">
  </head>
  <body>
    <h1>Welcome to the online Pokedex!</h1>
    <br>
    <div class="d-flex align-content-center justify-content-center flex-row">
      <form method="get" action="/">
        <button type="submit" value="Submit" class="btn btn-primary">Sort By Name</button>
      </form>
    </div>

    <div class="d-flex p-2 align-content-center justify-content-center flex-row flex-wrap">
      {{#each pokemons}}
        <div class="d-flex m-10 align-content-center justify-content-center flex-column poke">
          <img src={{this.img}} alt="">
          <p>#{{this.num}}</p>
          <h1>{{this.name}}</h1>
        </div>
  		{{/each}}
    </div>
  </body>
</html>
